# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build outputs — never commit these
dist
dist_electron
dist-ssr
build

# Local env files
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ─── WEBSITE (Download Site) ─────────────────────────────────────────────────
# The download website is a SEPARATE project and must NEVER be part of the
# Electron app repo. Host it separately (e.g. GitHub Pages on a different repo,
# Netlify, Vercel, etc.)
website/
index.website-root.html
